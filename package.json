{
  "name": "agents",
  "private": true,
  "type": "module",
  "scripts": {
    "check:shell": "node scripts/check-shell-size.mjs",
    "format": "trunk fmt",
    "lint": "trunk check --all",
    "test": "pnpm -r test",
    "test:scripts": "node --test scripts/tests/validate-skills/*.test.mjs",
    "validate:skills": "node scripts/validate-skills.mjs"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "mvdan-sh": "^0.10.1"
  },
  "packageManager": "pnpm@10.34.5",
  "engines": {
    "node": "^22.12.0 || >=24.0.0"
  }
}
